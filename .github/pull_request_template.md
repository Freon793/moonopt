## 变更内容

<!-- 做了什么，为什么这样做 -->

## 证据

<!-- 命令与结果、测试名、基准报告、示例输出 -->

```
moon check --deny-warn
moon test --deny-warn
```

## 边界与风险

<!-- 明确不做的部分；已知限制；数值/性能上的不确定性 -->

## 检查清单

- [ ] 新增算法带三类测试（单元 / 不变量 / 与 `oracle` 的差分，如适用）
- [ ] `.mbti` 变化是有意的（`moon info` 的 diff 已检查）
- [ ] `CHANGELOG.md` 已更新
- [ ] 未扩大 README / docs 中承诺的模型类别（能力缺失时返回 `NotSolved` + 原因）
